import 'package:flutter/cupertino.dart';

class InitialRoute extends StatelessWidget {
  final String route;
  const InitialRoute({super.key, required this.route});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
